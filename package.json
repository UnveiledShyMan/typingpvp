{
  "name": "typing-battle",
  "version": "1.0.0",
  "description": "Site de test de dactylo avec batailles 1v1",
  "main": "app.js",
  "type": "module",
  "scripts": {
    "install:all": "npm install && cd client && npm install && cd ../server && npm install",
    "dev": "node app.js",
    "dev:local": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd server && nodemon index.js",
    "dev:client": "cd client && npm run dev",
    "start": "node app.js",
    "start:server": "cd server && node index.js",
    "start:client": "cd client && npm run dev",
    "build": "cd client && npm install && npm run build",
    "build:client": "cd client && npm install && npm run build",
    "setup:db": "node server/db/setup-database.js",
    "setup": "npm run install:all && npm run setup:db && npm run build:client",
    "setup:plesk": "npm run install:all && npm run setup:db && npm run build:client",
    "postinstall": "cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["typing", "test", "battle", "1v1"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
